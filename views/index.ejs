<%- include('layout', {
  title: 'Home',
  user: user,
  cartCount: cartCount,
  wishlist: wishlist,
  body: `
<section>
  <h1>Products</h1>
  <div class="list">
    ${products
      .map(
        (p) => `
      <div class="item">
        <img src="${Object.values(p.images)[0][0]}" width="150">
        <h3>${p.title}</h3>
        <p>₹${p.price}</p>
        <a href="/product/${p.id}">View</a>
      </div>`
      )
      .join('')}
  </div>
</section>
` }) %>
